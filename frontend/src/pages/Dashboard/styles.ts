import styled from 'styled-components';

export const Container = styled.main`
  max-width: 1220px;
  padding: 1.5rem 1rem;
  margin-left: 0 auto;
`;

export const Title = styled.h1`
  font-size: 1.5rem;
  color: var(--blue);
  font-weight: 500;
  text-align: center;
`;
